This directory contains templates for extending the functionality of
spatialdata. This includes adding interfaces to new types of spatial
databases, such as seismic velocity models (see the spatialdb
directory).
